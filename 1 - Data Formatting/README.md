# Social Network Data Viewer

A simple Python project that reads social network data from a JSON file and displays users, their friends, and liked pages.

## Features

* Read data from a JSON file
* Display users and their connections
* Display available pages
* Uses Python's built-in `json` module

## Technologies

* Python
* JSON
* Jupyter Notebook
